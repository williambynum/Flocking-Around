<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Permissions for the radios are declared by the :radio module manifest and merged here;
      the camera and ARCore requirements come from :ar. Only app-level entries live in this
      file so the module boundaries stay honest.

      Note there is deliberately no usesPermissionFlags="neverForLocation" on the Wi-Fi and
      Bluetooth scan permissions: this app really does derive position from those signals.
    -->

    <application
        android:name=".SignalSurveyApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignalSurvey"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SignalSurvey">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
